import React, { useState } from "react";

const MainPage = (props) => {
    return (
        <div id="main-wrapper">
            Just some calcs I am working on to see what I can do and also work with React
            {/* stuff here */}
        </div>
    );
};

export default MainPage;
